# Copyright 2020 tel
# See LICENSE file for licensing details.
name: scscf
description: Serving CSCF decides whether UE’s sip message should be forwarded to application servers 
summary: SCSCF checks for the authentication data of UE and proceeds to forward the registration request  or SIP messages to the application servers only if the authentication data matches with the database.  
tags:
  - ims
series: [kubernetes]
deployment:
    type: stateful
    service: cluster
resources:
  image:
    type: oci-image
    description: docker image for SCSCF
    # upstream-source: "10.45.5.100:4200/canonical/ims_hss:v2.0"
provides:
  scscfip:
     interface: interface
