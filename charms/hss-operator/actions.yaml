# Copyright 2020 TataExlsi
# See LICENSE file for licensing details.
#
# This is only an example, and you should edit to suit your needs.
# If you don't need actions, you can remove the file entirely.
# It ties in to the example _on_fortune_action handler in src/charm.py
---
add-user:
    description: "Adding user in hss database"
    params:
        user:
            description: "sip username"
            type: string
            default: ""
        password:
            description: "sip password"
            type: string
            default: ""
        domain:
            description: "sip domain"
            type: string
            default: ""
        implicit:
            description: "implicit_set"
            type: integer
            default: ""
delete-user:
    description: "Delete user in hss database"
    params:
        user:
            description: "sip username"
            type: string
            default: ""
        password:
            description: "sip password"
            type: string
            default: ""
        domain:
            description: "sip domain"
            type: string
            default: ""
