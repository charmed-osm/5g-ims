# Copyright 2020 TataElxsi
# See LICENSE file for licensing details.
name: hss
description: Home Subscriber Subsystem  maintains user profile and location details. 
summary: HSS is the application used to maintain the user profile and authentication details. Other components in IMS connects with HSS if authentication details needs to be verified.
tags:
  - ims
series: [kubernetes]
deployment:
    type: stateful
    service: loadbalancer
resources:
  image:
    type: oci-image
    description: docker image for HSS
    # upstream-source: "10.45.5.100:4200/canonical/ims_hss:v2.0"
provides:
    hssip:
       interface: interface
